@model DoctorToothieApp.Controllers.ReserveDoctorVM
@{
}
<h2>Wybierz doktora</h2>
<div class="row">
    <div class="col-md-4">
        <form asp-action="Doctor">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="DoctorId" class="control-label"></label>

                <select asp-for="DoctorId" asp-items="Model.Doctors">
                </select>

                <span asp-validation-for="DoctorId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <a class="btn btn-primary" asp-action="Patient">Cofnij</a>
                <a class="btn btn-danger" asp-action="Cancel">Anuluj</a>
                <button type="submit" class="btn btn-success">Dalej</button>
            </div>
        </form>
    </div>
</div>